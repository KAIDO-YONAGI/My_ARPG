# Project Instructions

- Never remove, rewrite, or re-encode existing user-written comments unless the user explicitly asks for comment changes.
- When editing a file, preserve existing comments as-is whenever possible, even during refactors or file rewrites.
