# Docs 索引

本目录收纳工程的构建排障文档、架构图，以及分层重构的结论快照。

## 分层重构（已冻结）

| 文件 | 内容 |
|---|---|
| `My_ARPG_MVCS项目现状.md` | 已迁移的线、未迁移的域、已确认缺陷、测试现状、单例与执行序现状 |
| `My_ARPG_重构优化清单_未解决.md` | 冻结前未完成与待评估项（asmdef、xLua、效果数据驱动、多场景加载、GameJam 按需迁移等） |
| `My_ARPG_重构优化清单_已解决.md` | 已完成项的归档，含实现与验证记录 |

**来源与权威性**：这三份是工程外个人知识库的快照。

- 知识库路径 `D:\My_Docs\1TODOFiles\Learning\`，有独立的 git 仓库。
- 冻结时对应的知识库提交：`96083b0`。
- 知识库是活的权威源，本目录是 tag `arpg-arch-final` 时刻的快照；两者若有出入，以知识库为准。

工程无关的两份准则（`MVCS重构方法论.md`、`MVCS笔记.md`）没有拷进本仓库——它们不绑定具体工程，后续项目仍会继续使用，只存在于知识库；本仓库通过根 `README.md` 的「分层架构与重构现状」一节指向它们。

**冻结口径**：分层重构收束于玩家数值这条线，其余功能域不再迁移。唯一有意留下的验证点是技能域——它是「一次写要原子地改动两个数据聚合」这条准则目前唯一未被本工程验证的场景。`My_ARPG_重构优化清单_未解决.md` 里的排期状态停留在冻结前，未随冻结重新评估。

## 构建与排障

| 文件 | 内容 |
|---|---|
| `UnityAndroidBuildGuide.md` | Android 导出排障：环境自检、代理继承、lint 失败、StreamingAssets 非 ASCII 文件名 |
| `UnityAndroidBuildVerification.md` | Android Release 构建结果存档 |
| `UnityAndroidBuildDiagnostics.skill.md` | 上述排障的可复用技能稿 |

## 其他

| 位置 | 内容 |
|---|---|
| `UML/` | 七张系统架构图（A\* 寻路、玩家、敌人与 NPC、SO 与事件、场景与存读档、对话与背包、UI），每张各有一份 `.puml` 源码与 `.png` 渲染 |
| `开发日志.txt` | 开发过程记录 |
| `游戏指南.txt` | 玩法说明 |
