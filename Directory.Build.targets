<Project>
  <!--
    作用：把测试代码从 IDE（Rider / Visual Studio）的项目模型里摘掉。
    这样「查找用法 / Find Usages」不会再从 Assets/Tests 里搜出调用。

    为什么这样写：
      · Unity 生成的 *.csproj 每次重新生成都会被覆盖，直接改 csproj 无效；
      · 本文件位于工程根目录，Unity 不会重写它，MSBuild 每次求值都会读它；
      · MSBuild 在读取本文件时，csproj 里的 <Compile Include> 已经声明完毕，
        所以这里的 Remove 能真正生效。

    影响范围：仅 IDE 的项目模型。
      · Unity 自己的编译、以及 Unity Test Runner 完全不受影响（Unity 不读这个文件），
        测试照常能编译、能跑。
      · 代价：IDE 里对这些测试文件没有代码补全/跳转/重构（因为它们不在任何项目里）。

    想恢复：删掉本文件，然后在 Rider 里重新加载解决方案。
  -->
  <ItemGroup>
    <Compile Remove="Assets\Tests\**\*.cs" />
    <Compile Remove="$(MSBuildProjectDirectory)\Assets\Tests\**\*.cs" />
  </ItemGroup>
</Project>
